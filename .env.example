# 設定例。本番のシークレットはコミットしないこと（.env.local を使う）。
#
# Vite の base path。GitHub Pages のサブディレクトリに配置する場合に指定。
# 例: https://<user>.github.io/diary/ に置くなら VITE_BASE_PATH=/diary/
VITE_BASE_PATH=/

# GitHub バックアップのトークン等はアプリ内の「設定」画面で入力する。
# 環境変数ではなく IndexedDB に保存する設計（AGENTS.md 参照）。
