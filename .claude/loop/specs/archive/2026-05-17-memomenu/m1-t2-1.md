# T2-1: MemoCalendar.tsx 新規

## 変更対象
- `src/features/log/MemoCalendar.tsx`（新規）

## シグネチャ
```ts
interface Props { onPick: (dateKey: string) => void }
export default function MemoCalendar({ onPick }: Props)
```

## 依存する既存コード（複製元）
- `src/features/bookshelf/Calendar.tsx`（全体複製ベース）
- T0-1 `getMemoDateSetInMonth`（`../../lib/db`）
- `./MemoCalendar.module.css`（T2-2）

## 実装概要
1. Calendar.tsx を複製
2. `getDateSetInMonth` → `getMemoDateSetInMonth`（import 差し替え）
3. `useNavigate` import と `navigate` 使用を削除
4. `onPick(day)` を書き換え:
   ```ts
   const key = `${year}-${String(month).padStart(2,'0')}-${String(day).padStart(2,'0')}`;
   if (!hitDates.has(key)) return; // 空メモ日は無反応（合意・Skeptic）
   onPick(key);
   ```
   （findPageByDate 呼び出しは削除）
5. year/month state・prev/next 月送り・cells 構築・ドット表示（hitDates.has）・DOW・isToday は流用
6. import css を `./MemoCalendar.module.css`

## 受入条件チェックリスト
- [ ] 🔵 メモがある日にドット表示（getMemoDateSetInMonth 反映）
- [ ] 🔵 ドットのある日タップで onPick(`YYYY-MM-DD`) 発火
- [ ] 🔵 ドット無し日タップで onPick 未発火・視覚フィードバックなし（無反応）
- [ ] 🔵 前月/次月送りで月境界（1↔12月）正しく繰上下げ
- [ ] 🔵 key は zero-pad ローカル `YYYY-MM-DD`（dateKey と一致）
- [ ] 🔵 Calendar.tsx は無変更／useNavigate 不参照
- [ ] 🔵 `npx tsc --noEmit` 通過

## Key Links（配線検証）
- `getMemoDateSetInMonth` 本番呼び出し: MemoCalendar useEffect（T0-1 の dead 関数解消）。
- `MemoCalendar` 本番配線: T2-3 で LogListPage モーダル内に `<MemoCalendar onPick={scrollToDate} />`。

## テスト方針
- T3-1 で LogListPage.test 経由（ドット日/空日/月境界/JST 境界）。任意で MemoCalendar 単体。

## 注意
- 視覚（ドット色/opacity/サイズ・月送り・fade）は Calendar と完全同一（静けさ・別管理は対象ストアのみ差）。
