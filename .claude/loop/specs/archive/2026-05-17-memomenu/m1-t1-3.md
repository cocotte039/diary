# T1-3: LogListPage MemoMenu+Fab 配線

## 変更対象
- `src/features/log/LogListPage.tsx`

## 依存する既存コード
- `BookshelfPage.tsx` L125-133（header に HeaderTabs+BookshelfMenu 並置作法）, L175（`<Fab/>` root 末尾）
- T0-2 `Fab`（from prop）, T1-1 `MemoMenu`
- 既存: HeaderTabs / styles / useState

## 実装概要
1. import: `MemoMenu` (`./MemoMenu`), `Fab` (`../bookshelf/Fab`)
2. `const [showCalendar, setShowCalendar] = useState(false);`
3. header 内（`<HeaderTabs />` の隣）に `<MemoMenu onOpenCalendar={() => setShowCalendar(true)} />`
4. root（styles.root div）末尾に `<Fab from="/log" />`
5. この Wave ではモーダル本体は未追加（T2-3 で追加）。showCalendar は state と open ハンドラのみ配線（未使用 lint 回避のため T2-3 まで `setShowCalendar` のみ使用＝開く側は配線済、表示は T2-3）。

## 受入条件チェックリスト
- [ ] 🔵 メモ画面ヘッダーに MemoMenu トリガー表示（HeaderTabs と並置）
- [ ] 🔵 ペン FAB 表示・タップで /log/new（state.from='/log'）→ 戻ると /log
- [ ] 🔵 「設定」導線がメモ画面から到達可能
- [ ] 🟡 showCalendar state 追加（T2-3 でモーダル表示に使用。Wave1 時点で lint エラーを出さない形に）
- [ ] 🔵 既存メモ一覧表示・グルーピング・空状態は不変
- [ ] 🔵 `npx tsc --noEmit` 通過

## Key Links（配線検証）
- `MemoMenu` 本番配線: LogListPage header。`Fab` 本番配線: LogListPage root 末尾。grep `MemoMenu` `Fab from` in LogListPage.tsx。

## テスト方針
- T3-1 で LogListPage.test に統合。

## 注意
- 🟡 自律判断: Wave1 単独で lint/tsc を通すため、showCalendar 未使用警告が出る場合は T2-3 を同 Build セッション内で連続実装し解消（Wave1→2 連続）。判断理由を AGENTS.md 記録。
- 日記側 BookshelfPage は触らない。Fab は import 参照のみ（移動しない）。
