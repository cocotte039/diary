# T2-5: LogListPage.module.css モーダルCSS複製

## 変更対象
- `src/features/log/LogListPage.module.css`

## 依存する既存コード（複製元）
- `BookshelfPage.module.css` `.calendarOverlay` / `.calendarPanel` / `.calendarClose` / `@keyframes fadeIn`（L86-132 付近）

## 実装概要
1. BookshelfPage.module.css の `.calendarOverlay`（fixed/inset:0/rgba(0,0,0,0.6)/z-index:100/fadeIn 200ms）, `.calendarPanel`（max-width:360px 等）, `.calendarClose`（44px タップ域）, `@keyframes fadeIn` をクラス名込みで LogListPage.module.css に複製
2. 任意: 日付 section に `scroll-margin-top: 0.5rem`（上端詰まり回避・静けさ範囲内）

## 受入条件チェックリスト
- [ ] 🔵 overlay z-index:100（FAB 50 より前面）
- [ ] 🔵 fadeIn 200ms（≤200ms・新規色なし）
- [ ] 🔵 BookshelfPage.module.css は無変更
- [ ] 🟡 section scroll-margin-top で上端に詰まりすぎない（任意・微調整）

## Key Links
- T2-3 モーダル DOM が参照する styles.calendarOverlay/Panel/Close。

## テスト方針
- 視覚 CSS のため実装先行。build/tsc 通過で足る。

## 注意
- 共通 CSS 切り出しはしない（複製方針・非目標）。
