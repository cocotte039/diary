# T2-3: LogListPage section id + カレンダーモーダル

## 変更対象
- `src/features/log/LogListPage.tsx`

## 依存する既存コード
- `BookshelfPage.tsx` L152-172（モーダルオーバーレイ作法: role=dialog/aria-modal/aria-label/背景click閉じ/closeボタン）
- T2-1 `MemoCalendar`
- 既存: showCalendar state（T1-3 で追加済）, groups map レンダ L61-68

## 実装概要
1. import `MemoCalendar`（`./MemoCalendar`）
2. 各日付 `<section key={dk}>` に `id={`memo-date-${dk}`}` 追加（L62 相当）
3. body 内（または root 内、BookshelfPage と同位置作法）に `showCalendar && (...)` のモーダルオーバーレイ:
   - 外側 div: className=styles.calendarOverlay, role="dialog", aria-modal="true", aria-label="カレンダー", onClick で `e.target===e.currentTarget` 時 setShowCalendar(false)
   - 内側 panel: styles.calendarPanel
   - close button: styles.calendarClose, aria-label="カレンダーを閉じる", onClick setShowCalendar(false), 文字 ×
   - `<MemoCalendar onPick={scrollToDate} />`（scrollToDate は T2-4）
4. CSS クラスは T2-5 で LogListPage.module.css に複製

## 受入条件チェックリスト
- [ ] 🔵 各日付 section に `id="memo-date-YYYY-MM-DD"`（dateKey と一致）
- [ ] 🔵 カレンダー項目→モーダル開（role=dialog 出現）
- [ ] 🔵 背景クリック / × ボタンで閉じる
- [ ] 🔵 モーダル内に MemoCalendar 描画
- [ ] 🔵 a11y: role=dialog/aria-modal/aria-label 付与（本棚同水準）
- [ ] 🔵 `npx tsc --noEmit` 通過

## Key Links（配線検証）
- `MemoCalendar` 本番配線: LogListPage モーダル内。`scrollToDate` は T2-4 で定義し onPick に接続。section id は T2-4 scrollToDate の getElementById 対象。

## テスト方針
- T3-1: モーダル開閉・section id 存在・MemoCalendar 描画。

## 注意
- Esc 閉じ useEffect は T2-4 で追加（cleanup 対称）。focus trap/scroll lock は付けない（本棚同水準）。
