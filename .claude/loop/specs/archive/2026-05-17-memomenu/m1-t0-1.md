# T0-1: db.ts getMemoDateSetInMonth 追加

## 変更対象
- `src/lib/db.ts`（追記のみ。スキーマ変更なし・DB_VERSION 据置）

## 新規関数シグネチャ
```ts
export async function getMemoDateSetInMonth(year: number, month: number): Promise<Set<string>>
```

## 依存する既存コード
- `getDateSetInMonth(year, month)` L535-549（コピー元・一字一句同型）
- `dateKey(iso)` L492-498（ローカル年月日 zero-pad）
- `db.getAll('memos')`（memos ストア）

## 実装概要
1. `getDateSetInMonth` をコピー
2. `db.getAll('pages')` → `db.getAll('memos')` に変更
3. ループ対象を `m`、判定を `m.createdAt` に変更（ローカル `new Date(m.createdAt)` の getFullYear()===year && getMonth()+1===month で `set.add(dateKey(m.createdAt))`）
4. それ以外のロジック（ローカル年月比較・Set 構築）は完全同型

## 受入条件チェックリスト
- [ ] 🔵 `getMemoDateSetInMonth(2026,5)` がその月に createdAt を持つメモの dateKey 集合を返す
- [ ] 🔵 メモ 0 件の月で空 Set
- [ ] 🔵 JST 境界（UTC深夜）でも getDateSetInMonth と同じローカル比較（ズレ無し）
- [ ] 🔵 既存 getDateSetInMonth は無変更
- [ ] 🔵 `npx tsc --noEmit` 通過

## Key Links（配線検証）
- 本番呼び出し元: T2-1 `MemoCalendar.tsx` の useEffect から呼ばれる（T2-1 完了時に grep 確認）。本タスク単独では定義のみで可（同 M1 内で配線）。

## テスト方針
- TDD 可: db テスト（既存に db レイヤ直接テストがあれば同型追加。無ければ MemoCalendar.test 経由間接でも可）。月境界・空月・JST 境界。

## 注意
- `replaceAllData` 等他関数に影響を与えない。export 追加のみ。
