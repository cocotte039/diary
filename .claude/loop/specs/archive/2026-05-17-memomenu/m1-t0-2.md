# T0-2: Fab.tsx from prop 化

## 変更対象
- `src/features/bookshelf/Fab.tsx`

## 新規シグネチャ
```ts
interface Props { from?: string }
export default function Fab({ from = '/' }: Props)
```

## 依存する既存コード
- 現状 L20: `navigate('/log/new', { state: { from: '/' } })` 固定
- `MemoEditorPage.tsx` L48-51: `fromState = location.state?.from ?? '/'`; 新規 backTo=fromState

## 実装概要
1. `interface Props { from?: string }` 追加
2. `export default function Fab({ from = '/' }: Props)`
3. `navigate('/log/new', { state: { from } })` に変更
4. SVG・className・CSS import は無変更

## 受入条件チェックリスト
- [ ] 🔵 引数なし `<Fab />` で state.from === '/'（本棚完全不変）
- [ ] 🔵 `<Fab from="/log" />` で state.from === '/log'
- [ ] 🔵 BookshelfPage.tsx の `<Fab />`（L175）は無変更で従来動作
- [ ] 🔵 `npx tsc --noEmit` 通過

## Key Links（配線検証）
- 本番呼び出し元: `BookshelfPage.tsx` L175 `<Fab />`（既存・既定値で不変）、T1-3 で `LogListPage.tsx` `<Fab from="/log" />` 追加。

## テスト方針
- Fab 単体テスト（新規 or 既存拡張, T3-1）: MemoryRouter で render→click→navigate state 検証。

## 注意
- Fab を bookshelf/ から移動しない（非目標）。CSS import 現状維持。
