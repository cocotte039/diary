# T3-1: テスト追加更新

## 変更対象
- `src/features/bookshelf/Fab.test.tsx`（新規 or 既存拡張）
- `src/features/log/LogListPage.test.tsx`（拡張）
- 任意: `src/features/log/MemoCalendar.test.tsx`
- 既存 `BookshelfPage.test.tsx` は非回帰確認のみ（変更しない）

## 依存する既存コード
- 既存 LogListPage.test.tsx の wipeDB/seedMemos/replaceAllData・MemoryRouter・github mock 作法
- 既存 BookshelfPage.test.tsx の FAB/カレンダーテスト作法

## 実装概要（テスト計画表＝plan.md 準拠）
共通: 全 LogListPage 系に `beforeEach(() => { Element.prototype.scrollIntoView = vi.fn() })`（jsdom 未実装対策）。

| 対象 | ケース | 確信度 |
|---|---|---|
| Fab | 引数なし→state.from='/'; from='/log'→'/log' | 🔵 |
| BookshelfPage | 既存 FAB 遷移テスト緑維持（非回帰・無変更） | 🔵 |
| LogListPage | MemoMenu trigger click 開 / 外部 pointerdown 閉 / Escape 閉 / 設定 Link→/settings | 🔵 |
| LogListPage | Fab 表示・/log/new 遷移 state.from='/log' | 🔵 |
| LogListPage | カレンダー項目 click→role=dialog 出現 / 背景click・Esc・×で閉 | 🔵 |
| LogListPage | ドット日 click→モーダル閉+scrollIntoView が `#memo-date-YYYY-MM-DD` で呼出 | 🟡 |
| LogListPage | ドット無し日 click→scrollIntoView 未呼出・モーダル開維持 | 🔵 |
| LogListPage | JST 境界シード（UTC深夜, 既存作法流用）でドット日と section id 一致 | 🔵 |
| MemoCalendar(任意) | 月境界 prev/next・ドット getMemoDateSetInMonth 反映 | 🟡 |

## 受入条件チェックリスト
- [ ] 🔵 上表ケースを実装し `npx vitest run` 全緑
- [ ] 🔵 既存テスト（BookshelfPage/MemoEditorPage/その他）非回帰
- [ ] 🔵 `npx tsc --noEmit` 通過
- [ ] 🔵 scrollIntoView は vi.fn() モックで検証（jsdom 例外回避）

## Key Links（配線検証）
- テストは Fab/MemoMenu/MemoCalendar/scrollToDate が本番コードパス（LogListPage/BookshelfPage）から呼ばれることを実レンダで担保。

## テスト方針
- 既存テストの追加・削除はしない（拡張のみ。BookshelfPage.test は読むだけ）。

## 注意
- テスト不通過ならコミットしない（3回リトライ失敗→スキップ＆AGENTS.md 記録）。
