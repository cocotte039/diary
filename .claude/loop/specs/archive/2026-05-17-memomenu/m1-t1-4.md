# T1-4: LogListPage.module.css header/body 調整

## 変更対象
- `src/features/log/LogListPage.module.css`

## 依存する既存コード
- `BookshelfPage.module.css` `.body` padding-bottom 値（FAB 分の余白）
- 既存 `.header`（L付近）, `.body`（overflow-y:auto）

## 実装概要
1. `.header` に `align-items: baseline;` 追加（本棚ヘッダーとの視覚一貫性 🟡）
2. `.body` の padding-bottom を BookshelfPage.module.css の `.body` 同値へ（FAB 最下段被り防止）。値は BookshelfPage.module.css を実読し一致させる。

## 受入条件チェックリスト
- [ ] 🟡 `.header` align-items:baseline（HeaderTabs と MemoMenu の縦位置整合）
- [ ] 🔵 `.body` 末尾メモが FAB に隠れない padding-bottom（本棚同値）
- [ ] 🔵 新規色追加なし・transition 追加なし
- [ ] 🔵 BookshelfPage.module.css は無変更

## Key Links
- T1-1/T1-3 の MemoMenu・Fab 配置に対応する余白調整（視覚タスク）。

## テスト方針
- 視覚 CSS のため実装先行。tsc/build 通過で足る。

## 注意
- 🟡 align-items:baseline は推奨案（plan 裁定）。本棚 `.app-header` 既存挙動を壊さないこと（LogListPage 固有クラスのみ変更）。
