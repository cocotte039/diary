# T1-2: MemoMenu.module.css 新規

## 変更対象
- `src/features/log/MemoMenu.module.css`（新規）

## 依存する既存コード（複製元）
- `src/features/bookshelf/BookshelfMenu.module.css`（全行複製・改変なし）

## 実装概要
- BookshelfMenu.module.css を内容そのまま複製（クラス名 root/trigger/menu/item 等も同名維持）。視覚を本棚メニューと完全一致させる。

## 受入条件チェックリスト
- [ ] 🔵 BookshelfMenu.module.css と同一クラス・同一値
- [ ] 🔵 新規色の追加なし（既存 CSS 変数のみ）
- [ ] 🔵 BookshelfMenu.module.css は無変更

## Key Links
- T1-1 `MemoMenu.tsx` が `import styles from './MemoMenu.module.css'` で参照。

## テスト方針
- 視覚 CSS のため実装先行（テスト不要）。tsc/build 通過で足る。

## 注意
- 共通 CSS への切り出しはしない（非目標・複製方針）。
