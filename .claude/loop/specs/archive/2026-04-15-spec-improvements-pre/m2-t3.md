# M2-T3: handleBeforeInput を文字数基準に

## 変更対象
- `src/features/editor/EditorPage.tsx` (handleBeforeInput)

## 実装概要
- 既存の `nextValue.split('\n').length > LINES_PER_PAGE` を `nextValue.length > CHARS_PER_PAGE` に変更
- 削除系 inputType の素通り（`if (!inserted) return;`）は維持
- IME 変換中はスキップ（既存）
- 対象は `current === PAGES_PER_VOLUME`（最終ページ）のみ

## 受入条件
- 🔵 60ページ目で 1200字到達後、1文字追加入力が preventDefault で無効化
- 🔵 削除は通る（1200字以下への削減可能）
- 🔵 composition 中は発動しない
- 🔵 59ページ目以前では発動しない（そちらは checkOverflowAndNavigate で自動送り）
