# M1-T1: FAB位置上昇 🔵

正: `.whiteboard/plan.md` §T1。

## 変更対象
`src/features/bookshelf/BookshelfPage.module.css` `.fab` L142

## 実装概要
- `bottom: max(1.25rem, calc(env(safe-area-inset-bottom) + 0.75rem))` → `bottom: max(3.5rem, calc(env(safe-area-inset-bottom) + 3rem))`
- L135-138 コメントブロックに親指リーチ意図を1行追記

## 受入条件
- [ ] 🔵 `.fab` の bottom が新値
- [ ] 🔵 right/width/height/色/transition 不変（静けさ維持）
- [ ] 🔵 `BookshelfPage.test.tsx` FAB テスト（aria-label/遷移）グリーン維持

## テスト方針
CSS値のみ。専用テスト追加不要、既存遷移テストで回帰検知。

## 注意
他 `.fab*` プロパティに触れない。最小差分。
