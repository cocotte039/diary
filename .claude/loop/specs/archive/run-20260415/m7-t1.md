# M7-T7.1 — 全体検証・README 更新

## 変更対象

- `README.md`
- プロジェクト全体（grep / lint / build / test）

## 前提

M1〜M6 が完了済み。

## 実装概要

### 1. 残骸 grep 確認

以下のキーワードの残留がゼロであること:
- `NewVolumeCard`
- `.newCard`
- `新しいノートを作る`（aria-label 文字列、テストも含む）
- `.calendarToggle`
- `カレンダーを開く` / `カレンダーを閉じる` は「× ボタンの aria-label」として残すが、それ以外の用途は削除

残っていたら削除または書き換え。

### 2. README.md 更新

- **罫線仕様**: 「15/30/45 行目は少し濃い線」を削除し「1 行ごとに通常罫線で統一」に書き換え
- **新ノート作成導線**: 「本棚の『＋ 新しいノート』カードからのみ」→「本棚ヘッダーのメニュー（ハンバーガー）から『新しいノート』を選択」
- **カレンダー記述**: 「カレンダー日付ジャンプ」を「本棚ヘッダーのメニューから『カレンダー』を選択（全画面モーダル）」に
- **ディレクトリ構成図**: `NewVolumeCard.tsx` を削除、`BookshelfMenu.tsx` / `BookshelfMenu.module.css` を追加
- **テスト数**: `npm run test -- --run` の結果を確認し、README に記載がある数値を更新

### 3. 全体検証

```bash
npm run lint
npm run build
npm run test -- --run
```

すべてグリーンであること。

## 受入条件チェックリスト

- [ ] 🔵 grep で `NewVolumeCard` / `.newCard` / `新しいノートを作る` / `.calendarToggle` の残留ゼロ
- [ ] 🔵 README の罫線・新ノート作成・カレンダー仕様が更新されている
- [ ] 🔵 ディレクトリ構成図が実態と一致
- [ ] 🔵 `npm run lint` 警告・エラーゼロ
- [ ] 🔵 `npm run build` 通過
- [ ] 🔵 `npm run test -- --run` 全グリーン

## テスト方針

既存テストの全グリーン確認のみ（新規追加なし）。

## Key Links（本番コードパスからの呼び出し）

- README は仕様文書、実行パスには含まれない

## 注意事項

- README は「初見の人が上から順に読んでセットアップ〜実行まで完了できる再現性」を維持
- AGENTS.md の記述にも整合性を確認
