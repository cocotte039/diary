# T8-3.1: db.ts に deleteVolume 実装 + テスト

## 変更対象
- `src/lib/db.ts`（新規関数追加）
- `src/lib/db.test.ts`（テスト追加）

## 実装概要
volume と関連する全 pages を原子的に削除する `deleteVolume(volumeId)` を追加する。active 冊を削除した場合は、残存冊のうち最大 ordinal の冊を active に昇格させる。

## 関数シグネチャ

```ts
export async function deleteVolume(volumeId: string): Promise<void>
```

## 実装案

```ts
export async function deleteVolume(volumeId: string): Promise<void> {
  const db = await getDB();
  const tx = db.transaction(['volumes', 'pages'], 'readwrite');
  const vStore = tx.objectStore('volumes');
  const pStore = tx.objectStore('pages');

  const target = await vStore.get(volumeId);
  if (!target) {
    await tx.done;
    return; // no-op
  }

  // 関連 pages を by-volume index で収集し削除
  const pagesIdx = pStore.index('by-volume');
  let cursor = await pagesIdx.openCursor(volumeId);
  while (cursor) {
    await pStore.delete(cursor.primaryKey);
    cursor = await cursor.continue();
  }

  // volume 削除
  await vStore.delete(volumeId);

  // active 削除時のリカバリ: 残存最大 ordinal を active に昇格
  if (target.status === 'active') {
    const all = await vStore.getAll();
    if (all.length > 0) {
      all.sort((a, b) => b.ordinal - a.ordinal);
      const promoted: Volume = { ...all[0], status: 'active' };
      await vStore.put(promoted);
    }
    // 全削除の場合は ensureActiveVolume が BookshelfPage 初期ロード時に復旧
  }

  await tx.done;
}
```

※ `Volume` 型と `getDB` 関数は db.ts 内で既に import/定義されているはず。既存のコードスタイルに合わせる。

## 受入条件（テストで検証）
- 🔵 指定 volume が削除される（`getVolume(id)` が undefined を返す）
- 🔵 関連 pages が全削除される（`getPagesByVolume(id)` が [] を返す）
- 🔵 他 volume の pages には影響しない
- 🔵 存在しない id で no-op（throw しない）
- 🔵 active 冊 + completed 冊の 2 冊構成で active を削除 → completed が active に昇格
- 🔵 冊 1 件のみを削除 → volumes ストアが空になる（BookshelfPage が ensureActiveVolume で復旧する前提）

## テストケース（db.test.ts 追加）

```ts
describe('deleteVolume', () => {
  beforeEach(async () => {
    await _resetDBForTests();
  });

  it('volume と関連 pages を削除する', async () => {
    // setup: 冊 1 + pages 3 を用意
    // act: deleteVolume(id)
    // assert: getVolume(id) === undefined, getPagesByVolume(id).length === 0
  });

  it('他 volume のデータに影響しない', async () => {
    // setup: 冊 A (pages 2) + 冊 B (pages 2)
    // act: deleteVolume(A.id)
    // assert: 冊 B の volume と pages は残る
  });

  it('存在しない id は no-op', async () => {
    await expect(deleteVolume('nonexistent-id')).resolves.toBeUndefined();
  });

  it('active 冊削除時は最大 ordinal の completed が active に昇格', async () => {
    // setup: 冊1 ordinal=1 completed, 冊2 ordinal=2 active
    // act: deleteVolume(冊2.id)
    // assert: getActiveVolume() が冊1 を返し status === 'active'
  });

  it('最後の 1 冊削除で volumes が空になる', async () => {
    // setup: 冊1 active のみ
    // act: deleteVolume(冊1.id)
    // assert: getAllVolumes() === []
  });
});
```

## 配線検証ポイント
- `deleteVolume` は M8-4 で `BookshelfPage.tsx` から呼び出される
- このタスク完了時点では UI 呼び出しはない（テストコード + import 未使用でも OK）
- ただし **export されていること** は必須（`grep -rn "export async function deleteVolume" src/lib/db.ts` で確認）

## 注意事項
- IndexedDB のトランザクション完了を必ず `await tx.done` で待つ
- 既存の `rotateVolume` の実装パターンに寄せる（同様に volumes ストアだけ触る既存例を参考）
- Volume 型の status は 'active' | 'completed' の 2 値想定（db.ts または types/index.ts を確認）
