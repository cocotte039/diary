# M1-T2: 新仕様テスト追加（RED）

## 目的

1200 字超でも自動遷移・ロックされない新仕様を検証するテストを追加し、RED（実装前は FAIL）を確認する。

## 変更対象

- `src/features/editor/EditorPage.test.tsx` 末尾に新 describe を追加

## 依存する既存コード

- `CHARS_PER_PAGE` / `PAGES_PER_VOLUME`（`src/lib/constants.ts`）
- `ensureActiveVolume`, `savePage`（`src/lib/db.ts`）
- テストヘルパ `renderAt` / `renderWithLocationProbe`（EditorPage.test.tsx 内既存）

## 実装概要

plan.md D9.2 に示された以下 5 件のテストを追加する:

```tsx
describe('EditorPage: no auto-navigation nor final-page lock (char-limit-removal)', () => {
  it('1201 字入力しても遷移せず、text はそのまま保持される', async () => { ... });
  it('5000 字を一気に入力しても遷移しない', async () => { ... });
  it('最終ページで 1201 字入力しても preventDefault されない', async () => { ... });
  it('日付挿入で 1200 字超になっても現ページに留まる', async () => { ... });
  it('1200 字超でも進捗バー aria-valuenow は 100 固定', async () => { ... });
});
```

具体コードは `.whiteboard/plan.md` D9.2 参照。

## 受入条件チェックリスト

RED 確認（T3 実装前）:

- [ ] 🔵 新 describe が EditorPage.test.tsx に追加されている
- [ ] 🔵 `1201 字入力しても遷移せず...` テストが **FAIL** する（旧仕様では自動遷移するため）
- [ ] 🔵 `5000 字を一気に入力しても遷移しない` テストが **FAIL** する
- [ ] 🔵 `最終ページで 1201 字...preventDefault されない` テストが **FAIL** する
- [ ] 🔵 `日付挿入で 1200 字超...現ページに留まる` テストが **FAIL** する
- [ ] 🔵 `進捗バー aria-valuenow は 100 固定` テストは **PASS**（progressPct は既に clamp 済み、回帰防止目的）
- [ ] 🔵 既存テストの PASS 数は減らない

## テスト方針

このタスクは「テストを書いて RED 確認」するタスク。TDD の Red フェーズ。

## 注意事項

- ヘルパ関数 `renderAt` / `renderWithLocationProbe` は既存のものをそのまま使う（新規に作らない）。
- import で `CHARS_PER_PAGE` `PAGES_PER_VOLUME` が未使用なら追加する。
- RED 確認後、T3 で実装を完了させて GREEN にする。**このタスク完了時点では意図的に FAIL テストが残る**ため、コミットしない or コミットするなら次タスクとの依存を明示してプッシュはしない。

## コミットメッセージ

`[M1-T2] test(editor): 1200字超でも遷移しない新仕様テスト追加（RED）`
