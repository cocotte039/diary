# M1-T1: 旧仕様テスト削除

## 目的

自動次ページ遷移・最終ページロックの撤廃に伴い、関連する旧仕様テストを削除する。

## 変更対象

### `src/features/editor/EditorPage.test.tsx`

削除するテスト:

| describe / it | 行範囲（plan.md 時点） | 備考 |
|---|---|---|
| `EditorPage IME composition guard (M6-T2)` の 3 件 | L412-438, L453-463 | `composition 中の PageDown は遷移しない`（L440-451）は**残す** |
| `EditorPage auto next-page on overflow (M6-T3)` describe 全体（6 テスト） | L466-551 | describe ごと削除 |
| `EditorPage final page lock (M6-T4)` describe 全体（5 テスト） | L553-651 | describe ごと削除 |
| `date insertion` の `日付挿入で 1200 字を超える場合、次ページへ自動遷移する` | L808-823 | 1 件のみ削除 |

**注意**: 行番号は plan.md 時点。実ファイルでは grep で名前ベースに検索して削除すること。

### `src/lib/pagination.test.ts`

- `describe('splitAtCharLimit (M10)', ...)` 全体（約 L89-137） 削除

## 依存する既存コード

- なし（純粋にテスト削除）

## 実装概要

1. `EditorPage.test.tsx` を開き、上記 4 箇所を削除
2. `pagination.test.ts` の `splitAtCharLimit` describe を削除
3. `splitAtCharLimit` の import が test で残っていたら削除
4. `npm run test:run` が緑で終わることを確認（実装はまだ旧仕様のままだが、テストが消えたので自動遷移・最終ロックの検証がなくなる＝検証されなくなるだけで失敗はしない）

## 受入条件チェックリスト

- [ ] 🔵 `EditorPage.test.tsx` の `auto next-page on overflow` describe が消えている
- [ ] 🔵 `EditorPage.test.tsx` の `final page lock` describe が消えている
- [ ] 🔵 `EditorPage.test.tsx` の `IME composition guard` で 3 件削除・1 件（`composition 中の PageDown`）残存
- [ ] 🔵 `EditorPage.test.tsx` の `日付挿入で 1200 字を超える場合、次ページへ自動遷移する` が消えている
- [ ] 🔵 `pagination.test.ts` の `splitAtCharLimit` describe が消えている
- [ ] 🔵 `npm run test:run` が緑（このタスク単体で確認）
- [ ] 🔵 `npm run typecheck` が緑

## テスト方針

このタスクはテスト削除なので TDD 対象外。

## 注意事項

- describe 全体を削除する際、import 文の参照が減るので未使用 import がないか確認する（Vitest 等）。
- `composition 中の PageDown は遷移しない`（L440-451）は**必ず残す**。これは IME ガードの PageUp/PageDown 用仕様でまだ有効。

## コミットメッセージ

`[M1-T1] test(editor): 1200字自動遷移・最終ページロック関連テスト削除`
