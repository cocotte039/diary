# M4-T5: github.test BACKUP_PATH_RE非マッチ・memos非破壊

## 変更対象
- `src/lib/github.test.ts`（テスト追加のみ、本体変更なし想定）

## 実装概要
回帰防御テストを追加:
1. `BACKUP_PATH_RE` が `memos/2026-05-17.md` に**非マッチ**（importFromGitHub の tree 解析で memos/*.md がスキップされ volumes 復元に混入しない, E1誤import回避）。
2. `importFromGitHub` 実行後、ローカル既存 memos の件数が不変（U2: replaceAllData が memos 引数省略呼出のため memos ストア無触, E15）。

## 受入条件
- 🔵 `BACKUP_PATH_RE.test('memos/2026-05-17.md')===false`、`volumes/001-xxx/page-01.txt===true`（既存）維持
- 🔵 importFromGitHub（モック tree に volumes と memos 混在）→ memos ストア件数不変、volumes/pages のみ置換
- 🔵 既存 github.test 全緑（回帰なし）
- 🔵 `npm run lint && npm run test:run && npm run build` 全緑

## テスト方針
github.test.ts に上記2ケース追加。importFromGitHub は既存テストのモック octokit 構築を流用。テストの削除・既存改変はしない（追加のみ）。

## Key Links
replaceAllData(memos省略, M1-T3)/BACKUP_PATH_RE（既存）。本タスクはテストのみ＝回帰ガード。

## 注意
本体に変更が必要と判明した場合（例: importFromGitHub が誤って memos に触れる実装になっていた）は fix として最小修正し AGENTS.md 記録。原則テスト追加のみ。
