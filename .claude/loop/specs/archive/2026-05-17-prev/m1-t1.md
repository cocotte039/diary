# M1-T1: Memo型 / ExportPayload.memos / 定数

## 変更対象
- `src/types/index.ts`
- `src/lib/constants.ts`

## 実装概要
1. `types/index.ts` に `Memo` interface 追加（`SyncStatus`/`ISODateString` 既存型流用）。
2. `ExportPayload` に `memos: Memo[]` を**必須**フィールドで追加。コメントで「v2+。読み手は payload.memos ?? [] でフォールバック（旧v1互換）」と明記。
3. `constants.ts`: `DB_VERSION` 2→3、`EXPORT_FORMAT_VERSION` 1→2。

```ts
export interface Memo {
  id: string;
  content: string;
  createdAt: ISODateString;
  updatedAt: ISODateString;
  syncStatus: SyncStatus;
}
```

## 受入条件
- 🔵 `Memo` 型が export され他モジュールから import 可能
- 🔵 `ExportPayload.memos: Memo[]` 追加、TS strict でコンパイル通過
- 🔵 `DB_VERSION===3`、`EXPORT_FORMAT_VERSION===2`
- 🔵 `npm run lint` 緑（既存参照箇所の型エラーは後続タスクで解消されるため、本タスク単独では型エラーが db.ts/export.ts に出る想定 → M1-T2/T3, M4-T1 で解消。コミットは型エラーが残るなら M1-T2 とまとめて GREEN コミット）

## テスト方針
型のみ。専用テストなし。constants.test.ts が既存なら DB_VERSION/EXPORT_FORMAT_VERSION の数値直書きアサーションがないか確認し、あれば更新。

## Key Links
M1-T2/T3（db.ts）, M4-T1（export.ts）が本型を消費。本番接続は後続。

## 注意
- 新規色トークン・新規定数の追加は不要。既存定数の数値変更のみ。
- `ExportPayload` 変更で `export.ts`/`db.ts replaceAllData` に一時型エラーが出る。M1-T1 単独コミットせず M1-T2 と合わせて lint 緑にしてからコミットしてよい（plan ロールバック方針: M1 は内部のみ）。
