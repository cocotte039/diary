# M3-T2: BookshelfPage h1→HeaderTabs置換（回帰更新）

## 変更対象
- `src/features/bookshelf/BookshelfPage.tsx`
- `src/features/bookshelf/BookshelfPage.test.tsx`（回帰アサーション更新）

## 実装概要
`.app-header` 内の `<h1 className={styles.title}>本棚</h1>` を `<HeaderTabs />` に置換。BookshelfMenu は維持。styles.title が他で未使用なら CSS から削除（未使用警告回避、ただし他参照あれば残す）。

## 受入条件
- 🔵 BookshelfPage に h1「本棚」テキストが無く HeaderTabs が表示される
- 🔵 BookshelfMenu（ハンバーガー）は従来通り表示・機能（新しいノート/カレンダー/設定）
- 🔵 既存 BookshelfPage.test.tsx の「本棚」h1 を参照するアサーションを HeaderTabs 表示確認に**更新**（削除でなく代替確認）
- 🔵 既存本棚機能（カード一覧/長押し削除/カレンダー）回帰なし
- 🔵 `npm run test:run` 緑

## テスト方針
既存 BookshelfPage.test.tsx を実読し、`getByText('本棚')` 等の h1 依存箇所を HeaderTabs のタブ（role/link「本棚」）確認に置換。テストの追加・削除でなく**更新**（plan: テスト削除禁止、回帰の代替確認）。

## Key Links
HeaderTabs(M3-T1)← を BookshelfPage が render。

## 注意
h1 削除でレイアウト（.app-header の 3要素配置等）が崩れないか目視 Verify 対象。タブとハンバーガーの併存配置を維持。
