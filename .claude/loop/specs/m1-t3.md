# M1-T3: EditorPage 自動遷移/ロック削除（GREEN）

## 目的

EditorPage から自動次ページ遷移・最終ページロックを撤廃し、M1-T2 の新仕様テストを GREEN にする。

## 変更対象

- `src/features/editor/EditorPage.tsx`

## 依存する既存コード

- 既存の `handleChange` / `handleCompositionStart` / `handleCompositionEnd` / `insertDate` / `goPage`
- `isComposingRef`（残置）

## 実装概要

plan.md D1-D4 に従い、以下を削除する:

1. L26: `import { splitAtCharLimit } from '../../lib/pagination';`
2. L94-96: `pendingCursorPosRef` の宣言
3. L125-136: 初期ロード useEffect 内の pending 復元ブロック
4. L190-243: `checkOverflowAndNavigate` 関数（JSDoc 含む）
5. L266: `handleChange` 内の `checkOverflowAndNavigate(value);` 呼び出し
6. L285-291: `handleCompositionEnd` 内の `checkOverflowAndNavigate(e.currentTarget.value);` 呼び出し
   - 残すのは `isComposingRef.current = false;` のみ
7. L293-332: `handleBeforeInput` 関数（JSDoc 含む）
8. L408-409: `insertDate` 内の `checkOverflowAndNavigate(nextValue);` 呼び出しと IME ガード分岐
9. L552: `onBeforeInput={handleBeforeInput}` 属性

**残すもの**:
- `isComposingRef` / `handleCompositionStart` / `handleCompositionEnd`（ref 更新のみに縮小）
- `CHARS_PER_PAGE` の import（進捗バー計算で使用）
- 進捗バー `Math.min(100, Math.round((text.length / CHARS_PER_PAGE) * 100))`

## 配線検証（Key Links）

実装後、以下を grep で確認:
- `grep -r checkOverflowAndNavigate src/` → 0 件
- `grep -r pendingCursorPosRef src/` → 0 件
- `grep -r handleBeforeInput src/features/editor/` → 0 件
- `grep -n onBeforeInput src/features/editor/EditorPage.tsx` → 0 件
- `grep -r splitAtCharLimit src/features/` → 0 件（import 側）
- `grep -n isComposingRef src/features/editor/EditorPage.tsx` → 複数件残る（スワイプ・キー遷移・composition で使用）

## 受入条件チェックリスト

- [ ] 🔵 M1-T2 の 5 テスト全てが PASS（GREEN）
- [ ] 🔵 既存テストが全て PASS（回帰なし）
- [ ] 🔵 `npm run typecheck` エラーなし（未使用 import / unused-ref 警告なし）
- [ ] 🔵 grep で `checkOverflowAndNavigate` / `pendingCursorPosRef` / `handleBeforeInput` が EditorPage.tsx に残っていない
- [ ] 🔵 `isComposingRef` が **残っている**（スワイプ L456・PageUp/PageDown L440 の IME ガードに必要）
- [ ] 🔵 進捗バー `aria-valuenow` が 100 clamp されたままになっている
- [ ] 🔵 JSDoc から「自動遷移」「最終ページロック」への言及が削除されている

## テスト方針

T2 の RED テストを GREEN にする（TDD Green フェーズ）。

## 注意事項

- `handleCompositionEnd` は本体が `isComposingRef.current = false;` のみの関数になる。冗長に見えても削除しない。
- `useLayoutEffect` import は残す（textarea 自動伸張で使用）。
- 削除後の import 文で未使用のものがあればそれも削除する。

## コミットメッセージ

`[M1-T3] feat(editor): 1200字自動次ページ遷移と最終ページロックを撤廃`
