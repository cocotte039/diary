# M1-T3: MemoEditorPage.test.tsx notebook クラス不在テストを反転更新

## 変更対象
- `src/features/log/MemoEditorPage.test.tsx`（L155-158 付近）

## 実装概要
現状テストは textarea className が notebook クラスを**持たない**ことを固定している（`expect(ta.className).not.toMatch(/notebook-textarea|notebook-surface/)` 等）。R1（仕様変更）により textarea は notebook クラスを持つのが正となったため、当該アサーションを反転:
`expect(ta.className).toMatch(/notebook-surface/)` および `/notebook-textarea/` を持つことを検証。テスト名（it/describe）を「notebook クラスを持つ（日記と同一体験）」へ改名。

## 依存する既存コード
- M1-T1 の className 変更が前提（wave 2）

## 受入条件
- [ ] 🔵 当該テストが notebook-surface/notebook-textarea の**存在**を検証
- [ ] 🔵 テスト名が体験統一を表す文言に更新
- [ ] 🔵 MemoEditorPage.test.tsx 全緑
- [ ] 🔵 他テストは追加・削除・変更なし（本ケースのみ仕様変更に伴う反転更新）
- [ ] 🔵 full-suite 回帰なし

## テスト方針
これ自体がテスト更新。`npx vitest run src/features/log/MemoEditorPage.test.tsx` 緑を確認。

## 注意事項
テスト**削除**ではなく**反転更新**。コミットメッセージに「R1 仕様変更（メモを日記と体験統一）に伴う検証反転＝正当」と明記。テスト規約（削除禁止）と矛盾しないことを担保。
