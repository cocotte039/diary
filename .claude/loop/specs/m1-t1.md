# M1-T1: 設定リンク削除 + テスト書換

## 目的

EditorPage ヘッダー右端の `<Link to="/settings">設定</Link>` を削除する。本棚ハンバーガーメニュー経由の導線に一本化し、書く画面の静けさを強化する。

## 変更対象

- `src/features/editor/EditorPage.tsx` (L487-497 付近の `.headerRight`)
- `src/features/editor/EditorPage.test.tsx` (L95-103 付近の既存テスト)

## 依存する既存コード

- `src/features/bookshelf/BookshelfMenu.tsx` L63-68: 本棚メニューの「設定」リンクは既に存在（変更不要）
- `src/App.tsx`: `/settings` ルートは維持（BookshelfMenu から引き続きアクセスするため）
- `src/features/editor/EditorPage.module.css` `.headerRight`: `inline-flex + gap 0.25rem` で子要素数変化に堅牢

## 実装概要

1. `EditorPage.tsx` の `.headerRight` div から `<Link to="/settings" aria-label="設定" className="app-header-link">設定</Link>` の 1 行を削除
2. 日付挿入ボタン（`<button className={styles.headerDateButton}>`）は残す
3. `Link` の import 行は残す（本棚リンク L456 で使用中）
4. 既存テスト `header contains 本棚 link to /, current page number, and 設定 link` (L95-103) を書き換え、設定リンクが表示されないことを検証する

## 受入条件チェックリスト

- [ ] 🔵 `EditorPage.tsx` から `<Link to="/settings">` が削除されている
- [ ] 🔵 日付挿入ボタン（`aria-label="今日の日付を挿入"`）は残っている
- [ ] 🔵 本棚リンク（`aria-label="本棚に戻る"`）は残っている
- [ ] 🔵 書換後のテストで `screen.queryByRole('link', { name: '設定' })).not.toBeInTheDocument()` を検証
- [ ] 🔵 `npm run test:run` が全件緑
- [ ] 🔵 `npm run lint` が通る
- [ ] 🔵 BookshelfMenu の設定リンクは本番コードから参照されている（配線検証）

## テスト方針（Red→Green→Refactor）

1. **Red**: 既存テストを書き換える（設定リンクが無いことを assert）。現状の実装だと赤になる
2. **Green**: 実装コードから Link 削除 → テスト緑
3. **Refactor**: import 行の整理（今回は `Link` は残す）

書換後のテスト:

```tsx
it('header contains 本棚 link to /, current page number, 日付挿入ボタン (設定 link は無い)', async () => {
  const v = await ensureActiveVolume();
  renderAt(`/book/${v.id}/7`);
  expect(await screen.findByRole('link', { name: '本棚に戻る' })).toHaveAttribute('href', '/');
  expect(screen.getByRole('button', { name: '今日の日付を挿入' })).toBeInTheDocument();
  expect(screen.queryByRole('link', { name: '設定' })).not.toBeInTheDocument();
  expect(screen.getByTestId('page-indicator')).toHaveTextContent(`7 / ${PAGES_PER_VOLUME}`);
});
```

## 注意事項

- CSS は触らない（`.headerRight` は単一子要素でも視覚的破綻なし）
- BookshelfMenu は変更しない（既に設定リンクがある）
- 静けさ原則: トースト・通知・案内文は追加しない
