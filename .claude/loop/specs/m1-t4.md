# M1-T4: constants.test.ts / db.test.ts 書き換え

## 変更対象
- `src/lib/constants.test.ts`
- `src/lib/db.test.ts`

## 実装概要
- `LINES_PER_PAGE` を参照している箇所を `CHARS_PER_PAGE` or `LINES_PER_PAPER` に（用途に応じて）
- db.test.ts の「3ページ生成」は `'あ'.repeat(CHARS_PER_PAGE * 2 + 1)` のように書き換え
- constants.test.ts は新定数（CHARS_PER_PAGE=1200, LINES_PER_PAPER=60, PAGES_PER_VOLUME=60）を検証

## 受入条件
- 🔵 `npm run test -- src/lib/constants.test.ts src/lib/db.test.ts` が全緑
