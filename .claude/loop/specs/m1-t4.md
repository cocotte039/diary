# M1-T4: 一覧タップ背景ハイライト 🔵

正: `.whiteboard/plan.md` §T4。

## 変更対象
`src/features/log/LogListPage.module.css` `.row`(L38-43) / `.row:active`(L44-46) / L25付近コメント

## 実装概要
- `.row` に `transition: background-color 150ms ease;` 追加
- `.row:active` の `opacity: 0.8;` → `background-color: var(--color-accent);`（opacity 指定削除）
- コメントに「タップ時 accent 背景で控えめ触覚フィードバック（静けさ: 既存色・150ms）」

## 判断記録（要件逸脱の明示）
要件原文「`--color-rule` 系」だが `--color-rule`=rgba(255,255,255,0.08) は背景#1c1c20上で知覚困難。Aesthete 判断で既存 `--color-accent`(#3a3545) 採用（新規色禁止に適合・知覚可能・150msのみで過剰でない）。

## 受入条件
- [ ] 🔵 `.row:active` が `background-color: var(--color-accent)`、opacity 指定削除
- [ ] 🔵 `.row` に transition ≤200ms（150ms）
- [ ] 🔵 新規色なし（既存 `--color-accent` 使用）
- [ ] 🔵 `LogListPage.test.tsx` タップ遷移/長押しテスト グリーン維持

## テスト方針
CSS値。専用テスト不要、既存タップ遷移テストで回帰検知。border-radius 追加は見送り（スコープ最小）。
