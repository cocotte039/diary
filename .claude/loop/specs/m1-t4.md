# M1-T4: splitAtCharLimit 削除 + コメント整理

## 目的

T3 で EditorPage から参照されなくなった `splitAtCharLimit` 関数を削除し、関連コメントを新仕様に合わせて更新する。

## 変更対象

- `src/lib/pagination.ts`: `splitAtCharLimit` 関数削除
- `src/types/index.ts` L34: コメント修正
- `src/lib/db.ts` L213 付近: コメント補足

## 依存する既存コード

- T3 が完了し、EditorPage から `splitAtCharLimit` の import が削除されていること

## 実装概要

### pagination.ts

- L56-75 付近の `export function splitAtCharLimit(...)` を JSDoc ごと削除

### types/index.ts L34

- 変更前: `本文（\n 区切り、最大 CHARS_PER_PAGE=1200 文字）`
- 変更後: `本文（\n 区切り、目安 1200 文字、上限なし。進捗バー計算でのみ参照）`

### db.ts L213 付近

- `saveVolumeText` 関数の JSDoc コメントに注意書きを追加:
  - 追加行: `* 注意: 冊全文保存経路（DB 復元等）でのみ使用。通常の編集経路では savePage を使う。`

## 配線検証（Key Links）

- `grep -r splitAtCharLimit src/` → **0 件**（関数定義・import・テストすべてゼロ）

## 受入条件チェックリスト

- [ ] 🔵 `pagination.ts` から `splitAtCharLimit` 関数が削除されている
- [ ] 🔵 `src/` 配下に `splitAtCharLimit` の参照が残っていない（grep 0 件）
- [ ] 🔵 `types/index.ts` L34 のコメントが「目安 1200 文字、上限なし」に変更されている
- [ ] 🔵 `db.ts` の `saveVolumeText` JSDoc に注意書きが追加されている
- [ ] 🔵 `npm run test:run` 全緑
- [ ] 🔵 `npm run typecheck` エラーなし

## テスト方針

T1 で関連テスト（`splitAtCharLimit` describe）は既に削除済み。追加作業なし。

## 注意事項

- `pagination.ts` の他の関数（`getPageNumber`, `countLogicalLines`, `countPages`, `splitIntoPages`, `joinPages`, `getScrollTopForCursor`）は**残す**。
- `saveVolumeText` 本体の実装は**変更しない**（コメントのみ追加）。

## コミットメッセージ

`[M1-T4] refactor: splitAtCharLimit 削除 / 関連コメント整理`
