# M6-T6.1 — カレンダーモーダル化

## 変更対象

- `src/features/bookshelf/BookshelfPage.tsx`
- `src/features/bookshelf/BookshelfPage.module.css`
- `src/features/bookshelf/BookshelfPage.test.tsx`

## 前提

M4 (BookshelfMenu) と M5 (メニュー結線) が完了済み。

## 実装概要

1. 本棚下部の `.calendarToggle` ボタンブロックを削除
2. `{showCalendar && <Calendar />}` を fixed overlay + panel 構造のモーダルに差し替え
3. Esc で閉じる `useEffect` を追加
4. overlay 背景クリック / × ボタンで閉じる
5. CSS に `.calendarOverlay` / `.calendarPanel` / `.calendarClose` 追加、`.calendarToggle` 削除
6. テスト 3 件追加

## BookshelfPage.tsx 変更点

既存の `.calendarToggle` ボタンブロックを削除し、`{showCalendar && <Calendar />}` を以下に置換:

```tsx
{showCalendar && (
  <div
    className={styles.calendarOverlay}
    role="dialog"
    aria-modal="true"
    aria-label="カレンダー"
    onClick={(e) => {
      if (e.target === e.currentTarget) setShowCalendar(false);
    }}
  >
    <div className={styles.calendarPanel}>
      <button
        type="button"
        className={styles.calendarClose}
        aria-label="カレンダーを閉じる"
        onClick={() => setShowCalendar(false)}
      >×</button>
      <Calendar />
    </div>
  </div>
)}
```

Esc 閉じ:

```tsx
useEffect(() => {
  if (!showCalendar) return;
  const onKey = (e: KeyboardEvent) => {
    if (e.key === 'Escape') setShowCalendar(false);
  };
  document.addEventListener('keydown', onKey);
  return () => document.removeEventListener('keydown', onKey);
}, [showCalendar]);
```

## CSS 追加

```css
.calendarOverlay {
  position: fixed;
  inset: 0;
  background-color: rgba(0, 0, 0, 0.6);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 100;
  padding: 1rem;
  animation: fadeIn 200ms ease;
}
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
.calendarPanel {
  position: relative;
  background-color: var(--color-bg);
  border: 1px solid var(--color-rule);
  border-radius: 4px;
  padding: 1rem;
  max-width: 360px;
  width: 100%;
  max-height: calc(100dvh - 4rem);
  overflow-y: auto;
}
.calendarClose {
  position: absolute;
  top: 0.25rem;
  right: 0.25rem;
  width: 44px;
  height: 44px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 1.25rem;
  color: var(--color-text);
  opacity: 0.5;
  transition: opacity 120ms ease;
  background: transparent;
  border: none;
  cursor: pointer;
}
.calendarClose:active { opacity: 0.8; }
```

削除: `.calendarToggle` ブロック

## 追加テスト

```tsx
import { within } from '@testing-library/react';

describe('BookshelfPage calendar modal', () => {
  it('メニューの「カレンダー」項目で dialog が出現する', async () => {
    await ensureActiveVolume();
    renderPage();
    await screen.findByRole('link', { name: /ノート 1/ });
    await openMenu();
    fireEvent.click(screen.getByRole('menuitem', { name: 'カレンダー' }));
    expect(await screen.findByRole('dialog', { name: 'カレンダー' })).toBeInTheDocument();
  });

  it('× ボタンで dialog が閉じる', async () => {
    await ensureActiveVolume();
    renderPage();
    await screen.findByRole('link', { name: /ノート 1/ });
    await openMenu();
    fireEvent.click(screen.getByRole('menuitem', { name: 'カレンダー' }));
    const dialog = await screen.findByRole('dialog', { name: 'カレンダー' });
    fireEvent.click(within(dialog).getByRole('button', { name: 'カレンダーを閉じる' }));
    await waitFor(() => expect(screen.queryByRole('dialog')).toBeNull());
  });

  it('Esc キーで dialog が閉じる', async () => {
    await ensureActiveVolume();
    renderPage();
    await screen.findByRole('link', { name: /ノート 1/ });
    await openMenu();
    fireEvent.click(screen.getByRole('menuitem', { name: 'カレンダー' }));
    await screen.findByRole('dialog', { name: 'カレンダー' });
    fireEvent.keyDown(document, { key: 'Escape' });
    await waitFor(() => expect(screen.queryByRole('dialog')).toBeNull());
  });
});
```

## 受入条件チェックリスト

- [ ] 🔵 `.calendarToggle` ボタンブロックとスタイルが削除されている
- [ ] 🔵 モーダル構造（`role="dialog"` + `aria-modal="true"` + `aria-label="カレンダー"`）がある
- [ ] 🔵 × ボタン・背景クリック・Esc で閉じる
- [ ] 🔵 追加テスト 3 件がグリーン
- [ ] 🔵 既存テスト全グリーン
- [ ] 🔵 `npm run build` 通過

## Key Links（本番コードパスからの呼び出し）

- `setShowCalendar(true)` は `BookshelfMenu` の `onOpenCalendar` 経由で呼ばれる（M5 で結線済み）

## 注意事項

- Calendar 内で navigate → BookshelfPage が unmount → overlay 自動消滅
- z-index: overlay 100 / menu 50
