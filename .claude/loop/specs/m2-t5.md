# M2-T5: EditorPage.test.tsx を文字数基準に書き換え

## 変更対象
- `src/features/editor/EditorPage.test.tsx`

## 実装概要
- 「LINES_PER_PAGE+1 行で遷移」→「1201字で遷移」
- 「視覚行 overflow で遷移」系テストは削除（仕様廃止）
- 「composition 中は遷移しない」→ 1201字ベース
- 「60ページ目で overflow ロック」→ `CHARS_PER_PAGE + 1` 相当の入力でロック
- 「日付挿入で overflow」→ 1200字近くで日付スタンプ挿入して overflow
- テキスト構築は `'あ'.repeat(N)` など文字数で記述
- PAGES_PER_VOLUME=60 に合わせ、最終ページ判定ケースを調整

## 受入条件
- 🔵 `npm run test -- src/features/editor/EditorPage.test.tsx` が全緑
- 🔵 LINES_PER_PAGE の参照がテストから消えている
