# M7-T1: `--header-height` CSS 変数追加

## 目的
ヘッダーと本文 1 行目の重なり解消に向けて、ヘッダー高さを CSS 変数 / JS 定数で一元管理する。値は 2 行分 = `2 * LINE_HEIGHT_PX`。

## 変更対象
- ファイル:
  - `/home/cocotte/dev/diary/src/styles/global.css`
  - `/home/cocotte/dev/diary/src/lib/constants.ts`
- 追加:
  - CSS: `:root { --header-height: calc(2 * var(--line-height-px)); }`（= 57.6px）
  - JS: `export const HEADER_HEIGHT_PX = 2 * LINE_HEIGHT_PX;`

## 依存する既存コード
- `/home/cocotte/dev/diary/src/styles/global.css` 既存 `:root` の `--line-height-px`
- `/home/cocotte/dev/diary/src/lib/constants.ts` 既存 `LINE_HEIGHT_PX = FONT_SIZE_PX * LINE_HEIGHT_EM`

## 実装概要（3-5行）
1. `global.css` の `:root` ブロックに `--header-height: calc(2 * var(--line-height-px));` を追加。
2. `constants.ts` に `export const HEADER_HEIGHT_PX = 2 * LINE_HEIGHT_PX;` を追加。
3. 他箇所で使う場合は `var(--header-height)` を参照する（T7.2, T7.3 で使用）。

## 受入条件チェックリスト
- [ ] 🔵 `global.css` に `--header-height` が定義されている
- [ ] 🔵 `constants.ts` に `HEADER_HEIGHT_PX` が export されている
- [ ] 🔵 計算値が 57.6px (2 × 28.8px) となる

## Key Links（配線検証）
- `--header-height` は T7.2 で EditorPage の textarea padding-top に使用。
- `HEADER_HEIGHT_PX` は（必要なら）JS 側計算で参照可能。単なる定義追加なので直接の呼び出しは T7.2 以降で発生。

## テスト方針
- ユニット (Vitest):
  - `constants.ts` から `HEADER_HEIGHT_PX` を import し `toBe(57.6)`
  - CSS 変数は Vitest で直接計算できないため、T7.2 で統合時に目視確認
- 目視: DevTools で `:root` の `--header-height` が `57.6px` に解決されること。

## 注意事項
- 2 行固定は「ヘッダー高さが本文 1 行と 2 行目の境界線に重なり、本文は 3 行目から始まる」設計意図に基づく。安全側の値。
- safe-area-inset は T7.2 で padding-top に追加する（本タスクでは値そのものに含めない）。
