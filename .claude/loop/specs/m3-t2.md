# M3-T2: MemoMenu 設定Linkに state={{from:'/log'}}

## 変更対象
- `src/features/log/MemoMenu.tsx`

## 実装概要
設定 `<Link to="/settings" role="menuitem" ...>`（L57 付近）に `state={{ from: '/log' }}` prop を追加。メモ一覧から開いた設定の戻り先をメモ一覧 `/log` に。

## 依存する既存コード
- react-router-dom `Link`（import 済）。SettingsPage(M3-T3) が location.state.from で受領。

## 受入条件
- [ ] 🔵 設定 Link に `state={{ from: '/log' }}` が付与
- [ ] 🔵 カレンダー項目・menu 開閉挙動は不変
- [ ] 🔵 LogListPage.test.tsx 回帰なし

## Key Links
- 消費側: `SettingsPage.tsx`（M3-T3）→ 本番コードパス

## テスト方針
M3-T3 SettingsPage テストで間接検証。単体テスト追加なし。

## 注意事項
`to="/settings"` 不変、`state` 追加のみ。
