# M2-T2: checkOverflowAndNavigate 単純化

## 変更対象
- `src/features/editor/EditorPage.tsx` (checkOverflowAndNavigate)

## 実装概要
- 視覚行（scrollHeight）分岐（現 L191-L206）を削除
- `splitAtCharLimit(value)` のみを使う:
  ```ts
  const { keep, overflow } = splitAtCharLimit(value);
  if (overflow.length === 0) return;
  ```
- 以降の遷移処理（pendingCursorPosRef = overflow.length、setText(keep)、savePage、navigate）は既存ロジック踏襲

## 受入条件
- 🔵 1201字入力で自動次ページ遷移、前ページ keep=先頭1200、次ページ先頭 = overflow(+既存内容)
- 🔵 1200字ちょうどでは遷移しない
- 🔵 最終ページ（60ページ目）では checkOverflowAndNavigate は early-return
- 🔵 scrollHeight 分岐が削除されている
