# M4-T3: LogListPage 右スワイプ→/ 配線

## 変更対象
- `src/features/log/LogListPage.tsx`

## 実装概要
- import 追加: `useNavigate`（react-router-dom）、`useSwipeNavigation`（../../hooks/useSwipeNavigation）
- `const navigate = useNavigate();`
- `const swipe = useSwipeNavigation({ onSwipeRight: () => navigate('/'), disabled: showCalendar });`
- root `<div className={styles.root}>` に `{...swipe}` spread

## 依存する既存コード
- `showCalendar` state（既存）→ モーダル中無効
- `useSwipeNavigation`（M4-T1 完了前提）

## 受入条件
- [ ] 🔵 root div に swipe handlers 配線
- [ ] 🔵 水平右スワイプ（|dx|≥50, 水平優位, showCalendar=false）で navigate('/')
- [ ] 🔵 showCalendar=true で無反応
- [ ] 🔵 既存メモ一覧機能（MemoListItem/MemoMenu/MemoCalendar/Fab/HeaderTabs/scrollToDate）不変
- [ ] 🔵 LogListPage.test.tsx 回帰なし

## Key Links
- `useSwipeNavigation` を LogListPage 本番描画 root に spread＝本番コードパス呼出

## テスト方針
M4-T4 で右スワイプ→navigate('/') テスト追加。

## 注意事項
左スワイプは無反応（メモ一覧→本棚は右スワイプのみ。本棚→メモ一覧は左スワイプ。方向の一貫性: 本棚が左・メモが右の並びに対応＝HeaderTabs と同方向メンタルモデル）。
