# T2-4: LogListPage scrollToDate + Esc閉じ

## 変更対象
- `src/features/log/LogListPage.tsx`

## 依存する既存コード
- `BookshelfPage.tsx` L75-83（Esc 閉じ useEffect 同型）
- T2-3 のモーダル / section id `memo-date-${dk}`
- 既存 useCallback/useEffect import

## 実装概要
1. `scrollToDate` useCallback 追加:
   ```tsx
   const scrollToDate = useCallback((dk: string) => {
     setShowCalendar(false);
     requestAnimationFrame(() =>
       requestAnimationFrame(() =>
         document.getElementById(`memo-date-${dk}`)
           ?.scrollIntoView({ block: 'start', behavior: 'auto' })
       )
     );
   }, []);
   ```
   `<MemoCalendar onPick={scrollToDate} />` に接続（T2-3 のプレースホルダ差し替え）
2. Esc 閉じ useEffect（showCalendar 依存・add/removeEventListener 対称・cleanup 必須・BookshelfPage L75-83 同型）

## 受入条件チェックリスト
- [ ] 🔵 ドット日タップ→モーダル閉＋該当 section へ scrollIntoView 呼出
- [ ] 🟡 二重 rAF で state 反映後フレームに scroll（チラつき/競合回避・plan裁定）
- [ ] 🔵 対象 section 不在時 `?.` で例外なし（null 安全）
- [ ] 🔵 Escape でモーダル閉じる（cleanup 対称・StrictMode 安全）
- [ ] 🔵 空メモ日は MemoCalendar 側 early return で scrollToDate 未到達（無反応）
- [ ] 🔵 `npx tsc --noEmit` 通過

## Key Links（配線検証）
- `scrollToDate` 本番配線: `<MemoCalendar onPick={scrollToDate} />`（T2-3 モーダル内）。grep で onPick={scrollToDate} 確認。dead function 不可。

## テスト方針
- T3-1: `Element.prototype.scrollIntoView = vi.fn()` モック。ドット日 click→scrollIntoView が `#memo-date-YYYY-MM-DD` 要素で呼出。空日 click→未呼出・モーダル開維持。

## 注意
- 🟡 二重 rAF は plan 裁定（Skeptic C1 優先）。テストで 1 回 rAF で十分と判明すれば簡略化可・判断は AGENTS.md 記録。
