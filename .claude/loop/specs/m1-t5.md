# M1-T5: 右エッジスワイプで戻る 🟡

正: `.whiteboard/plan.md` §T5（差分コード全文あり）。

## 変更対象
`src/features/log/MemoEditorPage.tsx`（T2 と同ファイル、T2 完了後に連続実装）

## 実装概要
- ref追加: `touchStartXRef`/`touchStartYRef`(`useRef<number|null>(null)`)、`isComposingRef`(`useRef(false)`)
- 既存 `handleBack` の async 部を `goBack` 共通関数化（C2: flush→navigate を保持）:
```ts
const goBack = useCallback(async () => {
  try { await flush(); } catch { /* 保存失敗でも遷移継続 */ }
  navigate(backTo);
}, [flush, navigate, backTo]);
const handleBack = useCallback((e: React.MouseEvent) => {
  e.preventDefault(); void goBack();
}, [goBack]);
```
- textarea に `onCompositionStart`/`onCompositionEnd` で isComposingRef 切替
- `styles.root` div(L135) に `onTouchStart`/`onTouchEnd`（EditorPage L291-331 同型、plan.md §T5 のコード使用）
- import: `SWIPE_THRESHOLD_PX` from `../../lib/constants`
- 右スワイプ(dx>0, |dx|≥SWIPE_THRESHOLD_PX, |dx|>|dy|*2)→`void goBack()`。左スワイプ無反応（メモ1画面）

## Key Links（本番配線）
`onTouchStart`/`onTouchEnd` を return 文最上位 `styles.root` div(data-testid="memo-editor-page") に JSX prop 配線＝本番レンダリングパス直結。`goBack` は handleBack（戻るリンク onClick）と onTouchEnd 両方から呼ばれ dead code なし。

## 受入条件
- [ ] 🔵 右スワイプで `flush()` 後 `navigate(backTo)`
- [ ] 🔵 戻るリンク click も従来通り flush→navigate（goBack 経由・挙動不変）
- [ ] 🔵 IME 変換中はスワイプ無反応
- [ ] 🔵 左スワイプ無反応
- [ ] 🔵 水平閾値未満/縦優位スワイプ無反応
- [ ] 🟡 popstate ガード/StrictMode と非干渉（touch は JSX prop、addEventListener 不使用）

## テスト方針
T6 に集約（touch は createEvent + defineProperty で touches/changedTouches 強制）。

## 注意
EditorPage は変更しない（非目標）。スワイプ共通化フック抽出は見送り（コピー実装）。
