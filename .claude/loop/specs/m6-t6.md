# M6-T6: WritePage の「新しいノート」ボタン削除

## 目的
新冊作成が本棚からのみ可能になるため、WritePage 内の「新しいノート」ボタン（`handleRotate`）と関連 UI を除去する。WritePage 自体は M7 T7.5 で削除。

## 変更対象
- ファイル:
  - `/home/cocotte/dev/diary/src/features/write/WritePage.tsx`
  - `/home/cocotte/dev/diary/src/features/write/useWrite.ts`（`rotateNow` エクスポートは残すが UI からは呼ばれなくなる）
- 削除対象:
  - 「新しいノート」「次の冊へ」等のボタン JSX
  - `handleRotate` ハンドラ（未使用になれば削除、または関連 UI のみ削除）
- 保持:
  - `useWrite.rotateNow` は WritePage 以外から参照が無ければ削除可だが、M7 まで影響を広げない方針で UI のみ除去

## 依存する既存コード
- `/home/cocotte/dev/diary/src/features/write/WritePage.tsx`
- `/home/cocotte/dev/diary/src/features/write/useWrite.ts`

## 実装概要（3-5行）
1. `WritePage.tsx` を grep し「新しいノート」「rotate」に関連する button 要素を特定。
2. 該当 button とそのクリックハンドラ（`handleRotate` 等）を削除。
3. ビルド確認で未参照警告が出たら `rotateNow` の import も掃除。
4. CSS ルール（該当ボタンのスタイル）も削除もしくはコメントアウト。

## 受入条件チェックリスト
- [ ] 🔵 `/write` 画面に「新しいノート」「次の冊」等のボタンが存在しない
- [ ] 🔵 `npm run build` がエラー無しで通る
- [ ] 🔵 既存テストが全パス

## Key Links（配線検証）
- `WritePage.tsx` は T4.1 で `/write` ルートから参照されている本番経路（M7 まで残る）。
- ボタン削除後、WritePage の他機能（書く、autosave）に影響しないこと。

## テスト方針
- ユニット (Vitest + Testing Library):
  - WritePage をレンダ → 「新しいノート」「rotate」等のテキストを持つ button が 0 件
  - 既存 WritePage テスト（あれば）が全通過
- 目視: `/#/write` で該当ボタンが見えない。autosave は動く。

## 注意事項
- M7 T7.5 で WritePage ごと削除予定。本タスクはその前段として UI のみ除去。
- `useWrite.rotateNow` の扱いは最小差分原則で保留可（未使用警告が出たら削除）。
