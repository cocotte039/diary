# M3-T1: BookshelfMenu 設定Linkに state={{from:'/'}}

## 変更対象
- `src/features/bookshelf/BookshelfMenu.tsx`

## 実装概要
設定 `<Link to="/settings" role="menuitem" ...>`（L63 付近）に `state={{ from: '/' }}` prop を追加。本棚から開いた設定の戻り先を本棚 `/` に。

## 依存する既存コード
- react-router-dom `Link`（既に import 済）。`state` prop は location.state として SettingsPage(M3-T3) が読む。

## 受入条件
- [ ] 🔵 設定 Link に `state={{ from: '/' }}` が付与
- [ ] 🔵 他の Link/button・onClick・menu 挙動は不変
- [ ] 🔵 BookshelfPage.test.tsx 回帰なし

## Key Links
- 消費側: `SettingsPage.tsx`（M3-T3）が `useLocation().state.from` で受領 → 本番コードパス

## テスト方針
M3-T3 の SettingsPage テストで間接検証。本タスク単体のテスト追加なし（回帰確認のみ）。

## 注意事項
`to="/settings"` は不変、`state` 追加のみ。
