# M4-T4: swipe テスト追加（hook 単体 + 2画面）

## 変更対象（追加のみ・既存テスト不変）
- 新規 `src/hooks/useSwipeNavigation.test.ts`（または .tsx）
- `src/features/bookshelf/BookshelfPage.test.tsx`（ケース追加）
- `src/features/log/LogListPage.test.tsx`（ケース追加）

## 実装概要
`MemoEditorPage.test.tsx` の fireTouch/swipe ヘルパ作法を流用（touchStart→touchEnd で clientX/Y を与える `createTouchEvent` 等）。

### hook 単体（useSwipeNavigation.test）
- 左スワイプ（dx=-80, dy=0）→ onSwipeLeft 呼ばれる / onSwipeRight 呼ばれない
- 右スワイプ（dx=+80, dy=0）→ onSwipeRight 呼ばれる
- |dx|<50（dx=-30）→ どちらも呼ばれない
- 縦優位（dx=-80, dy=-200）→ 呼ばれない
- disabled=true → 呼ばれない
- testing: renderHook で handlers を取得し、合成 TouchEvent オブジェクトを渡して呼び出す

### BookshelfPage.test
- 左スワイプで navigate('/log')（useNavigate mock or MemoryRouter + ルート確認）
- showCalendar 表示中（カレンダー開）→ 左スワイプ無反応

### LogListPage.test
- 右スワイプで navigate('/')
- showCalendar 中 → 無反応

## 依存する既存コード
- M4-T1/T2/T3 完了前提（wave 3）
- 既存テストの render/router セットアップ・fireTouch 作法

## 受入条件
- [ ] 🔵 hook 単体: 左/右/閾値未満/縦優位/disabled の5挙動を検証
- [ ] 🔵 BookshelfPage: 左スワイプ→/log、showCalendar 中無反応
- [ ] 🔵 LogListPage: 右スワイプ→/、showCalendar 中無反応
- [ ] 🔵 既存テスト（MemoEditorPage/EditorPage の swipe 等含む）追加・削除・変更なし、全緑
- [ ] 🔵 死蓄なし: useSwipeNavigation が本番2画面で呼ばれることを grep 確認

## テスト方針
`npx vitest run src/hooks src/features/bookshelf/BookshelfPage.test.tsx src/features/log/LogListPage.test.tsx` 緑。最後に full-suite 回帰。

## 注意事項
TouchEvent は jsdom で部分対応。既存 MemoEditorPage.test の合成イベント作法（changedTouches/touches 配列を手で組む）に厳密に倣う。fileParallelism:false 既設のため副作用注意は既存どおり。
