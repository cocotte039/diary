# M3-T3: App.tsx に /log=LogListPage ルート

## 変更対象
- `src/App.tsx`

## 実装概要
`<Route path="/log" element={<LogListPage />} />` を `/log/new`・`/log/:memoId` より**前**に追加（react-router v6 は exact マッチなので順序非依存だが可読性のため /log → /log/new → /log/:memoId 順）。M3-T4 で LogListPage 実体化、本タスクはスタブ可。

## 受入条件
- 🔵 `/log` で LogListPage がマウント（スタブ可）
- 🔵 `/log/new`・`/log/:memoId`（M2）と競合せず正しく解決
- 🔵 既存ルート回帰なし
- 🔵 `npm run lint` 緑

## テスト方針
App ルーティング回帰は M3-T4 LogListPage.test で実質カバー。

## Key Links
LogListPage(M3-T4)←。HeaderTabs の `to="/log"` 着地先。

## 注意
`*`→`/` フォールバックは最後尾維持。
