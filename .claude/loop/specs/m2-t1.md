# M2-T2.1 — 日付挿入時の scrollTop 保持

## 変更対象

- `src/features/editor/EditorPage.tsx` (`insertDate` 関数内)
- `src/features/editor/EditorPage.test.tsx` (テスト追加)

## 依存する既存コード

- `surfaceRef`: `useRef<HTMLDivElement | null>(null)` — `.surface` スクロールコンテナ参照
- `textareaRef`: textarea 参照
- `checkOverflowAndNavigate`: 1200 字超過時の自動次ページ遷移
- `onSelectionChange`: カーソル位置保存
- `formatToday`: 日付スタンプ生成

## 実装概要（TDD）

1. **Red**: テストを先に追加 → 失敗を確認
2. **Green**: `insertDate` に scrollTop 保存・復元ロジックを追加
3. **Refactor**: JSDoc 更新

## 変更後コード (insertDate)

```ts
const insertDate = useCallback(() => {
  const el = textareaRef.current;
  if (!el) return;
  const stamp = formatToday();
  const start = el.selectionStart ?? el.value.length;
  const end = el.selectionEnd ?? el.value.length;
  const nextValue = el.value.slice(0, start) + stamp + el.value.slice(end);
  // .surface (外側スクロールコンテナ) の scrollTop を保存。
  // focus() / setSelectionRange() は React 再レンダリング後に scrollTop を
  // リセットする副作用を持つため、rAF 後に明示的に復元する。
  const savedScrollTop = surfaceRef.current?.scrollTop ?? 0;
  setText(nextValue);
  const nextPos = start + stamp.length;
  requestAnimationFrame(() => {
    const cur = textareaRef.current;
    if (!cur) return;
    cur.focus();
    const clamped = Math.max(0, Math.min(nextPos, cur.value.length));
    cur.setSelectionRange(clamped, clamped);
    if (surfaceRef.current) surfaceRef.current.scrollTop = savedScrollTop;
  });
  onSelectionChange(nextPos);
  if (isComposingRef.current) return;
  checkOverflowAndNavigate(nextValue);
}, [onSelectionChange, checkOverflowAndNavigate]);
```

## 追加テスト

`src/features/editor/EditorPage.test.tsx` の該当 describe 内に追加:

```ts
it('日付挿入後、surface の scrollTop が保持される', async () => {
  // 既存の render ヘルパを使う
  // ... (既存テストと同じセットアップ)
  const textarea = screen.getByRole('textbox') as HTMLTextAreaElement;
  await act(async () => {
    fireEvent.change(textarea, { target: { value: 'あ'.repeat(200) } });
  });
  const surface = document.querySelector('[data-testid="editor-surface"]') as HTMLElement;
  surface.scrollTop = 200;
  textarea.setSelectionRange(10, 10);
  const btn = screen.getByRole('button', { name: '今日の日付を挿入' });
  await act(async () => {
    fireEvent.click(btn);
    await new Promise<void>((r) => requestAnimationFrame(() => r()));
  });
  expect(surface.scrollTop).toBe(200);
});
```

※ `data-testid="editor-surface"` が既に EditorPage.tsx の `.surface` div に設定されているか確認。無ければ追加する。

## 受入条件チェックリスト

- [ ] 🔵 日付挿入前 `surface.scrollTop = 200` が挿入後も 200 のまま
- [ ] 🔵 既存の「日付スタンプ挿入」「カーソル位置に挿入」テストがグリーン
- [ ] 🔵 「1200 字超えで自動次ページ遷移」テストがグリーン
- [ ] 🔵 `npm run build` 通過
- [ ] 🔵 JSDoc に「scrollTop 保持の意図」が追記されている

## Key Links（本番コードパスからの呼び出し）

- `insertDate` は `EditorPage.tsx` 内のヘッダー button の `onClick` で本番配線されている
- `surfaceRef` は `EditorPage.tsx` の `<div className={styles.surface} ref={surfaceRef}>` で使用

## 注意事項

- `surfaceRef.current?.scrollTop` のアクセス順: 保存は setText 前 / 復元は rAF 内で textarea 操作後
- オーバーフロー時は navigate 発火で surface が unmount → scrollTop 設定は無害
