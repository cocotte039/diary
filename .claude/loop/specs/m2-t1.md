# M2-T1: EditorPage.tsx import 更新・不要定数削除

## 変更対象
- `src/features/editor/EditorPage.tsx`

## 実装概要
- import から `LINES_PER_PAGE` 削除、`CHARS_PER_PAGE` を追加
- `splitAtLine30` import を `splitAtCharLimit` に
- ローカル定数 `PAGE_HEIGHT_PX = LINES_PER_PAGE * LINE_HEIGHT_PX` を削除（M2-T4 で使わなくなる）
- 以降のコードで `LINES_PER_PAGE` / `LINE_HEIGHT_PX` の参照が残っていないか確認（罫線計算は CSS に任せる）

## 受入条件
- 🔵 ビルドエラーなく compile 通過（他タスクで参照が消えた前提）
- 🔵 import が `CHARS_PER_PAGE`, `PAGES_PER_VOLUME`, `SWIPE_THRESHOLD_PX`, `splitAtCharLimit`
