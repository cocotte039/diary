# M1-T6: typecheck + test:run 全緑確認

## 目的

T1-T5 の実装完了後、全体のビルド・型・テストが通ることを確認する。

## 変更対象

なし（検証のみ）。

## 実装概要

以下のコマンドを順に実行し、全て成功することを確認する:

```bash
npm run typecheck
npm run test:run
npm run build
```

## 受入条件チェックリスト

- [ ] 🔵 `npm run typecheck` exit 0（TypeScript エラー 0 件）
- [ ] 🔵 `npm run test:run` 全テスト PASS
- [ ] 🔵 `npm run build` 成功（Astro + Vite ビルド完走）
- [ ] 🔵 テスト数の差分: 削除 約 15 件 / 追加 5 件 = 純減 約 10 件（`pagination.test.ts` の削除分も含む）

## テスト方針

既存テストの PASS 数に加え、M1-T2 で追加した 5 テストが全て PASS することを確認。

## 注意事項

- FAIL があったら fix_queue 起票ではなく**その場で最大 3 回まで修正**（Build Agent の許容範囲）。
- 4 回以上 FAIL が続くようなら `.claude/loop/AGENTS.md` に記録して fix_queue 起票。

## コミットメッセージ

このタスク単体ではコミット不要（検証のみ）。
